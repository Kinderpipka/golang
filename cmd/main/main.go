package main

import (
	"github.com/Kinderpipka/golang/internal/app"
)

func main() {
	// a := []int{1, 3, 234, 5, 6, 71}
	// a := []string{"s", "f", "g"}
	// x := "sasd"
	// app.Ykazatel()
	// app.Radius()
	// app.Nedel()
	// fmt.Println(app.Contains(a, x))
	// (app.GetMax(a...))
	app.Sozdanie()
}
